{
  "name": "school-record-management-system",
  "version": "1.0.0",
  "private": true,
  "description": "Serverless functions backing the Personalized School Record Management System's /api endpoints (verify-master, save). The rest of the project is static HTML/JS with no build step.",
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
